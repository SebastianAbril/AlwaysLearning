const languages = {
    fra: "French",
    kon: "Kikongo",
    lin: "Lingala",
    lua: "Tshiluba",
    swa: "Swahili"
    }

for( const [key,value] of Object.entries(languages)){
    console.log(key);
    console.log(value);
}